import { OtherTodoState } from "./types"

const state: OtherTodoState = {
    id: 0,
    matches: false,
    isInSearch: false,
    alertMsg: false,
    alertMsgSpace: false
}

export const otherTodo = {
    state,
}